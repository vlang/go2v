module main

fn main() {
	mut strings := ['hello', 'world']
	for idx, el in strings {
		println('${idx} ${el}')
		println('${idx} ${strings[idx]}')
	}
	for idx, _ in strings {
		println(idx)
	}
	for _ in strings {
		println('hello')
	}
	for i, _ in [2]isize{} {
		for j, _ in [2]isize{} {
			println('hello2')
		}
	}
}
