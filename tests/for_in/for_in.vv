module main

fn main() {
	mut strings := ['hello', 'world']
	for idx, el in strings {
		println(idx)
		println(el)
		println(idx)
		println()
	}
	for idx, _ in strings {
		println(idx)
	}
	for _ in strings {
		println('hello')
	}
}
