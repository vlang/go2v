module main

fn contains_value(m map[KK]TT, v TT) bool {
	for x in m {
		if x == v {
			return true
		}
	}
	return false
}

// Arbitrary types for K, T.
type KK = string
type TT = isize

fn main() {
	mut m := {
		'uno':  1
		'dos':  2
		'tres': 3
	}
	mut v := t(5)
	mut ok := contains_value(m, v)
	print('m contains value ${v}: ${ok}\n')
	v = 3
	ok = contains_value(m, v)
	print('m contains value ${v}: ${ok}\n')
}
