module main

import math

fn round(x f64) isize {
	mut y := isize(math.floor(x + 0.5))
	return y
}

fn main() {
	for x in [f64(-1.1), -0.9, -0.5, -0.1, 0.0, 0.1, 0.5, 0.9, 1.1] {
		print('${x:5.1} ${round(x):5}\n')
	}
}
