module main

type EE = string

fn main() {
	mut x := {
		'a': struct {}
		'b': struct {}
		'c': struct {}
	}
	mut e := EE('b')
	x.delete(e)

	for t, _ in x {
		print('x contains the element "${t}" \n')
	}
}
