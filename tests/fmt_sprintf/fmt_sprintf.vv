module main

fn main() {
	mut name := 'V'
	mut count := isize(7)
	mut formatted := 'name="${name}" count=${count:02} type=${typeof(count).name} hex=${count.hex().to_upper()} %'
	print('-> ${formatted}')
	mut err := error('bad "${name}"')
	println(err)
}
