module main

import io.util
import os

fn main() {
	for filepath in ['/tmp/foo.txt', '/tmp/bar.txt', '/tmp/foo.txt'] {
		mut err := os.remove(filepath)
		if isnil(err) {
			println('Removed ${filepath}')
		} else {
			(os.stderr()).write_string('${err}\n') or { 0 }
		}
	}
}

fn init() {
	mut err := ioutil.write_file('/tmp/foo.txt', r'abc'.bytes(), 644)
	if !isnil(err) {
		panic(err)
	}
}
