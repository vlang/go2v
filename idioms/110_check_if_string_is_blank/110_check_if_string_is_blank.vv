module main

fn main() {
	for _, s in ['', 'a', ' ', '\t \n', '_'] {
		mut blank := s.trim_space() == ''
		if blank {
			print('"${s}" is blank\n')
		} else {
			print('"${s}" is not blank\n')
		}
	}
}
