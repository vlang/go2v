module main

import strconv

fn main() {
	mut a := ['11', '22', '33']
	mut b := []isize{len: a.len}
	mut err := IError{}

	for i, s in a {
		mut err_1_ok := true
		err_1 := if err_1_ok { none } else { error('conversion failed') }
		if err_1 != unsafe { nil } {
			panic(err_1)
		}
	}
	println(b)
}
