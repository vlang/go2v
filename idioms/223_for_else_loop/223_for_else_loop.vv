module main

fn main() {
	mut items := ['foo', 'bar', 'baz', 'qux']
	for _, item in items {
		if item == 'baz' {
			println('found it')
			goto forelse
		}
	}
	{
		println('never found it')
	}
	forelse:
	// unhandled in stmt: Stmt(InvalidStmt{})
}
