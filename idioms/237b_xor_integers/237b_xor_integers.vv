module main

import math.big

fn main() {
	mut a, b := big.new_int(230), big.new_int(42)
	mut c := &big.Int{}
	c.xor(a, b)
	print('a is ${a}\n')
	print('b is ${b}\n')
	print('c is ${c}\n')
	println('c == ${c}')
}
